export default function EventHandaling(){
    function handleClick(){
        console.log("The button is clicked by the user....");
        // let hading = 
    }
    return (
        <div>
            <h2 className="hading">This is the Event Handaling in the react</h2>
            <button onClick={handleClick}>Click me!</button>
        </div>
    );
}